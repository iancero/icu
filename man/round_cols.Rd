% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounding.R
\name{round_cols}
\alias{round_cols}
\title{Round the numeric columns of a data frame}
\usage{
round_cols(df, digits = 3, min = 0)
}
\arguments{
\item{df}{A data frame (or tibble).}

\item{digits}{A single whole number: decimal places to round to.
Defaults to 3.}

\item{min}{A single non-negative, finite number: the smallest magnitude
any rounded value may have (see \code{\link[=round_with_min]{round_with_min()}}). The default
\code{min = 0} means plain rounding.}
}
\value{
The data frame with numeric columns rounded, otherwise
unchanged (same class, same column order).
}
\description{
Applies \code{\link[=round_with_min]{round_with_min()}} to every numeric column of a data frame,
leaving all other columns untouched. A convenience for making model
output and summary tables readable at a glance.
}
\details{
With \code{min > 0}, the minimum applies to \emph{every} numeric column, and any
column containing exact zeros will have them set to \code{min}, with one
warning per such column. If some columns have meaningful zeros (counts,
differences), round those separately rather than raising \code{min} here.

Integer columns are numeric and so are processed; if \code{min} binds on
one, the affected column is converted to double.
}
\examples{
df <- data.frame(
  term = c("age", "sex"),
  estimate = c(1.23456, -0.98765),
  p_value = c(0.00001, 0.04321)
)
round_cols(df, digits = 3)
round_cols(df, digits = 3, min = 0.001)

}
