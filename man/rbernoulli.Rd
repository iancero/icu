% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random.R
\name{rbernoulli}
\alias{rbernoulli}
\title{Draw from a Bernoulli distribution}
\usage{
rbernoulli(n, prob)
}
\arguments{
\item{n}{A single non-negative whole number: the number of draws.}

\item{prob}{Probability of success (a 1). Either a single value applied
to all draws, or a vector of length \code{n} giving a per-draw probability.
Other lengths are an error: unlike base R's \verb{r*} functions, no partial
recycling is done. \code{NA} values are an error.}
}
\value{
An integer vector of length \code{n} containing \code{0}s and \code{1}s.
}
\description{
Generates random 0/1 draws, i.e. a single Bernoulli trial per draw.
A convenience wrapper around \code{\link[=rbinom]{rbinom()}} with \code{size = 1}.
}
\details{
Returns integer \code{0}/\code{1}, not logical \code{TRUE}/\code{FALSE} (as the now-deprecated
\code{purrr::rbernoulli()} did). Use \code{as.logical()} on the result if needed.

Unlike \code{\link[=rbinom]{rbinom()}}, which produces \code{NA} draws with a warning when \code{prob}
contains \code{NA}, this function errors: an \code{NA} probability in a simulation
usually indicates an upstream problem.
}
\examples{
rbernoulli(10, 0.5)

# Per-draw probabilities, e.g. subject-specific from a logistic model
rbernoulli(3, prob = c(0.1, 0.5, 0.9))

# Simulating a binary outcome for a dataset
n <- 20
age <- rnorm(n, mean = 60, sd = 10)
p <- plogis(-4 + 0.06 * age)
outcome <- rbernoulli(n, p)

}
