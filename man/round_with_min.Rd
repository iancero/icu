% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounding.R
\name{round_with_min}
\alias{round_with_min}
\title{Round with a minimum magnitude}
\usage{
round_with_min(x, digits, min)
}
\arguments{
\item{x}{A numeric vector to round.}

\item{digits}{A single whole number: decimal places to round to,
passed to \code{\link[=round]{round()}}.}

\item{min}{A single non-negative, finite number: the smallest magnitude
the result may have. Applies symmetrically to negative values,
using the sign of the original (pre-rounding) value. \code{min = 0}
disables the minimum entirely, making the function equivalent
to \code{\link[=round]{round()}}.}
}
\value{
A numeric vector the same length as \code{x}.
}
\description{
Rounds like \code{\link[=round]{round()}}, but values whose rounded magnitude would fall
below \code{min} are clamped to \code{min} instead (keeping their sign). The
motivating case is p-values: with \code{digits = 3} and \code{min = 0.001}, a
p-value of 0.00001 returns 0.001 rather than 0.
}
\details{
Exact zeros in \code{x} are set to \code{min}, with a warning (suppressable via
\code{\link[=suppressWarnings]{suppressWarnings()}}). If your data contains meaningful zeros, this
function is probably not what you want. (With \code{min = 0}, zeros are
left alone and no warning is issued.)

The clamp applies to any value whose rounded magnitude is below \code{min},
not only values that round to zero: \code{round_with_min(0.004, 3, 0.01)}
returns 0.01.

\code{NA} and \code{NaN} propagate unchanged; \code{Inf} and \code{-Inf} pass through
untouched.

Note that the numeric result 0.001 overstates a p-value of 0.00001;
for manuscript tables the string \code{"<0.001"} is usually the right
presentation. This function is for numeric pipelines, not display.
}
\examples{
round_with_min(c(0.032, 0.00001), digits = 3, min = 0.001)
round_with_min(-0.0004, digits = 3, min = 0.001)  # sign preserved

}
