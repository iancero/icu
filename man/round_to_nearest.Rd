% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounding.R
\name{round_to_nearest}
\alias{round_to_nearest}
\title{Round to the nearest multiple of a unit}
\usage{
round_to_nearest(x, unit, round_func = round)
}
\arguments{
\item{x}{A numeric vector to round.}

\item{unit}{A single positive, finite number: the multiple to round to.}

\item{round_func}{The rounding function to apply. Defaults to \code{\link[=round]{round()}};
\code{\link[=floor]{floor()}} or \code{\link[=ceiling]{ceiling()}} give "always down" / "always up" behavior.}
}
\value{
A numeric vector the same length as \code{x}, rounded to multiples
of \code{unit}.
}
\description{
Rounds values to the nearest multiple of \code{unit}, e.g. to the nearest
0.5, 5, or 100. The rounding function can be swapped out to round
up or down instead.
}
\details{
With the default \code{round_func}, halfway cases follow base \code{\link[=round]{round()}}'s
round-half-to-even ("banker's rounding") behavior, so
\code{round_to_nearest(0.25, 0.1)} returns 0.2, not 0.3.

\code{NA}, \code{NaN}, and \code{Inf} are handled the same way as in \code{\link[=round]{round()}}:
they propagate through unchanged.
}
\examples{
round_to_nearest(c(1.2, 3.7, 6.1), 0.5)
round_to_nearest(137, 5)
round_to_nearest(137, 5, round_func = floor)
round_to_nearest(c(2.3, NA, 8.9), 0.5)  # NA propagates

}
